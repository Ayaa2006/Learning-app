import React from "react";
import { Container, Typography } from "@mui/material";

const Progress = () => {
  return (
    <Container sx={{ mt: 5 }}>
      <Typography variant="h4">Suivi de Progression 📈</Typography>
      <Typography variant="body1">Graphique des statistiques ici...</Typography>
    </Container>
  );
};

export default Progress;
